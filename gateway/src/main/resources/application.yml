spring:
  application:
    name: api-gateway
server:
  port: 443
spring.cloud.gateway:
  discovery:
    locator:
      enabled: true
      lower-case-service-id: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/