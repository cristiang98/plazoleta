server:
  port: 8082


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/plazas
    username: postgres
    password: 9807

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: plazas

eureka:
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka
appDescription: "Pragma Plaza service"
appVersion: "1.0.0"
