server:
  port: 8081


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/users
    username: postgres
    password: 9807

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: users

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
appDescription: "Pragma users service"
appVersion: "1.0.0"

logging:
  level:
    org:
      springframework:
        security=DEBUG:
